@model IEnumerable<WebApplication.Subreviewer>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.id_paper_assignment)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.id_user)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.invitation_send_date)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.invitation_ack)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.is_accepted)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.id_paper_assignment)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.id_user)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.invitation_send_date)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.invitation_ack)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.is_accepted)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id_subreviewer }) |
            @Html.ActionLink("Details", "Details", new { id=item.id_subreviewer }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id_subreviewer })
        </td>
    </tr>
}

</table>
